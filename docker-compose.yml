version: '3.8'

services:
  eureka:
    build:
      context: .
      dockerfile: Dockerfile
    image: eureka
    container_name: eureka
    ports:
      - "8761:8761"
    networks:
      - devsecops-network

networks:
  devsecops-network:
    external: true
